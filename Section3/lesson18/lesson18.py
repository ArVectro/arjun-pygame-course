def path(velocity, time):
   return(velocity*time) # if we do print(velocity*time) we get 'None' as well because it is printing the print(velocity*time)

#S = path(30,60) would also add a none to the output
print(path(30,60))
